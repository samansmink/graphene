# Quick Start

```sh
# create symlink to benchmark runner in Documents/duckdb/build/release dir and build the final manifest, note that it is SGX only currently (it it though?)
make

# run the duckdb benchmark program
SGX=1 ./pal_loader benchmark_runner Q06